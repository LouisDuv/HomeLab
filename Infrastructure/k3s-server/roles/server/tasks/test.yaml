- name: = Test = Get k3s service status
  ansible.builtin.systemd:
    name: k3s
  register: k3s_service_status

- name : Setting variables
  ansible.builtin.set_fact:
    k3s_service : "{{ k3s_service_status.status.ActiveState }}"